<!DOCTYPE html>
<html lang="en">
<head>
    <script>
        if (localStorage.getItem('dark-mode') === 'true' || (!('dark-mode' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.querySelector('html').classList.add('dark');
        } else {
            document.querySelector('html').classList.remove('dark');
        }
        </script>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/flowbite@2.4.1/dist/flowbite.min.css"  rel="stylesheet" />
    <link rel="stylesheet" href="/tailwind.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test server</title>
</head>
<body>
    <div class="flex flex-col h-screen justify-between">
        <%- include('partials/header') %>
        <main class="px-6 py-8 md:px-10 md:m-10">
            <h2 class="text-2xl font-bold mb-4">Welcome to the Promethee DSS Dashboard</h2>
            <p class="mb-6">This dashboard provides a comprehensive interface to the Promethee Decision Support System, allowing you to manage your decision-making process and analyze the results.</p>
            
            <section class="mb-8">
              <h3 class="text-xl font-bold mb-4">Key Features</h3>
              <ul class="list-disc pl-6">
                <li>Define and manage your decision criteria</li>
                <li>Input and compare your decision alternatives</li>
                <li>Specify your preferences and decision-making parameters</li>
                <li>View and analyze the Promethee ranking of your alternatives</li>
                <li>Generate detailed reports and visualizations</li>
              </ul>
            </section>
            
            <section>
              <h3 class="text-xl font-bold mb-4">Getting Started</h3>
              <p class="mb-4">To begin using the Promethee DSS dashboard, please sign in or create a new account. If you need any assistance, our support team is available to help.</p>
              <div class="flex justify-center space-x-4">
                <a href="/login" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Sign In</a>
              </div>
            </section>
          </main>
        <%- include('partials/footer') %>
    </div>
    <script>
        const lightSwitches = document.querySelectorAll('.light-switch');
            if (lightSwitches.length > 0) {
            lightSwitches.forEach((lightSwitch, i) => {
                if (localStorage.getItem('dark-mode') === 'true') {
                lightSwitch.checked = true;
                }
                lightSwitch.addEventListener('change', () => {
                const { checked } = lightSwitch;
                lightSwitches.forEach((el, n) => {
                    if (n !== i) {
                    el.checked = checked;
                    }
                });
                if (lightSwitch.checked) {
                    document.documentElement.classList.add('dark');
                    localStorage.setItem('dark-mode', true);
                } else {
                    document.documentElement.classList.remove('dark');
                    localStorage.setItem('dark-mode', false);
                }
                });
            });
            }
    </script>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/flowbite@2.4.1/dist/flowbite.min.js"></script>
</body>
</html>